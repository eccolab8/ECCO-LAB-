const search=document.querySelector('#search');const cards=[...document.querySelectorAll('.card')];const buttons=[...document.querySelectorAll('.filters button')];let active='todos';function render(){const q=(search?.value||'').toLowerCase().trim();cards.forEach(c=>{const okQ=!q||c.innerText.toLowerCase().includes(q);const okC=active==='todos'||c.dataset.cat===active;c.style.display=okQ&&okC?'':'none'})}search?.addEventListener('input',render);buttons.forEach(b=>b.addEventListener('click',()=>{buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active');active=b.dataset.cat;render()}));